/* Stub for the Next.js "server-only" marker package so Vitest can resolve
 * server-only modules in the node environment. The marker has no runtime
 * surface; importing it must be a no-op under test. */
export {};